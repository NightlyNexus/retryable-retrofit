apply plugin: 'java'

targetCompatibility = JavaVersion.VERSION_1_7
sourceCompatibility = JavaVersion.VERSION_1_7

dependencies {
  compile project(':retryable')
  compile deps.android
}

apply from: rootProject.file('gradle/gradle-mvn-push.gradle')
